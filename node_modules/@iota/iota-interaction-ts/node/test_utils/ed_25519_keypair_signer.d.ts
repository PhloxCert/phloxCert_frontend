import { PublicKey } from "@iota/iota-sdk/dist/esm/cryptography";
import { Ed25519Keypair } from "@iota/iota-sdk/keypairs/ed25519";
import { TransactionSigner } from "./iota_interaction_ts.js";
export declare class Ed25519KeypairSigner implements TransactionSigner {
    signer: Ed25519Keypair;
    constructor(signer: Ed25519Keypair);
    sign(tx_data_bcs: Uint8Array): Promise<string>;
    publicKey(): Promise<PublicKey>;
    iotaPublicKeyBytes(): Promise<Uint8Array>;
    keyId(): string;
}
